# Copy to web/.env (or set in Vercel project settings). These are PUBLIC values:
# the anon key is safe to ship because Row Level Security makes it read-only.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
